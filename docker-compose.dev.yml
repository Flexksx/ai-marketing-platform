name: ai-marketing-platform-dev

services:
  frontend:
    build:
      context: .
      dockerfile: apps/webapp/Dockerfile
      target: dev
    volumes:
      - ./apps/webapp/spa/src:/workspace/apps/webapp/spa/src
    ports:
      - "5173:5173"
